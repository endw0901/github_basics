# branching

```
// show local branch
git branch

// local + remote branchを表示
git branch -a

// rename branch
git branch -m before newname

// delete branch
git branch -d deletetargetBranch
git branch -a

// create new branch and switch to new branch
git checkout -b feature/xxxxx

```
