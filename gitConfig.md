# gitconfig

```
git config --global user.name "xxxx"
git config --global user.email "xx@xxx.com"
git config --global --list

cat ~/.gitconfig


cd ~
code .bash_profile
```

## bash_profile

```

```
